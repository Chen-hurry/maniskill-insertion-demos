seed: 42
env_config: configs/env/pick_place.yaml
method_config: configs/method/hybrid.yaml
output_dir: experiments/hybrid/default
data:
  nominal_dir: data/nominal
  anomaly_dir: data/anomalies
  recovery_dir: data/recovery
training:
  batch_size: 128
  learning_rate: 0.0003
  total_steps: 100000
  eval_interval: 5000
  checkpoint_interval: 10000
evaluation:
  episodes: 50
  save_videos: true
