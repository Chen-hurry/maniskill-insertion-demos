name: sorting
task_id: Sorting-v1
max_episode_steps: 300
observation_mode: rgbd
control_mode: pd_ee_delta_pose
success_threshold: 0.85
anomalies:
  enabled: true
  types: [object_swap, bin_occlusion, color_shift]
